\section{Kombinierte Regelung}
\subsection{Portierung des Balance-PID (Zander)}

Jonah Zanders Bachelorarbeit (2023) implementiert eine dreistufige Kaskadenregelung für ein selbstbalancierendes Fahrrad, die als Grundlage 
für die in dieser Arbeit entwickelte Simulationsregelung dient \citep{zander2023}. Die Hardware-Architektur umfasst einen BNO055-IMU-Sensor 
zur Neigungsmessung, einen BeagleBone-Black-Einplatinencomputer als Steuerungseinheit sowie separate Motorcontroller für Lenkung und Antrieb. 
Die Regelungsarchitektur gliedert sich in drei hierarchische Ebenen: eine primäre Balance-PID-Regelung (Angle-to-Steering), eine sekundäre 
Lenkpositionsregelung und eine vereinfachte Geschwindigkeitssteuerung.

\textbf{Primäre Balance-Regelung:} Das Kernstück von Zanders Implementierung bildet ein PID-Regler, der die Rollwinkelabweichung in einen 
Lenkwinkel-Sollwert umsetzt. Listing~\ref{lst:zander_pid_params} zeigt die von Zander gewählten PID-Parameter, die sich durch hohe 
proportionale (\(K_p = 10{,}0\)) und differentielle Verstärkungen (\(K_d = 2{,}2\)) bei deaktiviertem Integralanteil (\(K_i = 0{,}0\)) 
auszeichnen. Diese Parametrierung gewährleistet eine schnelle Reaktion auf Neigungsänderungen ohne die Gefahr des Aufschaukelns durch 
Drift-Integration.

\begin{lstlisting}[style=wbt_c,caption={PID-Parameter in Zanders Hardware-Implementierung},label={lst:zander_pid_params}]
//PID Angle to Steering
#define ANGLE_PID_KP                    10.0f
#define ANGLE_PID_KI                    0.0f
#define ANGLE_PID_KD                    2.2f 
#define ANGLE_PID_OUTPUT_MIN            -150.0f
#define ANGLE_PID_OUTPUT_MAX            150.0f
#define ANGLE_PID_INTEGRAL_MIN          -60.0f
#define ANGLE_PID_INTEGRAL_MAX          60.0f
\end{lstlisting}

Die PID-Berechnung in Zanders System implementiert eine zeitbasierte Integration und Differentiation mit Historien-Pufferung zur 
Rauschunterdrückung. Der Differentialterm wird über mehrere Zeitschritte gemittelt, um hochfrequente Störungen zu dämpfen, 
während der Integralterm durch Anti-Windup-Mechanismen begrenzt wird.

\textbf{Sekundäre Lenkpositionsregelung:} Die zweite Regelebene steuert den physischen Lenkmotor über einen MD49-Controller an, 
um den von der Balance-PID vorgegebenen Sollwinkel zu erreichen. Aufgrund der mechanischen Eigenschaften des Lenkantriebs 
(Reibung, Trägheit, Totzeit) sind hier deutlich höhere Verstärkungen erforderlich:

\begin{lstlisting}[style=wbt_c,caption={Lenkpositions-PID-Parameter in Zanders System},label={lst:zander_steering_pid}]
//PID Steering PID
#define MD49_PID_KP                     850.0f
#define MD49_PID_KI                     300.0f
#define MD49_PID_KD                     30.0f
#define MD49_PID_OUTPUT_MIN             -100000.0f
#define MD49_PID_OUTPUT_MAX             100000.0f
\end{lstlisting}

\textbf{Echtzeit-Threading-Architektur:} Zanders Implementierung nutzt eine multi-threaded Architektur mit unterschiedlichen 
Abtastraten: IMU-Datenerfassung mit 200\,Hz, Lenkpositionsregelung mit 1000\,Hz und Geschwindigkeitsregelung mit niedrigerer 
Frequenz. Watchdog-Mechanismen überwachen die Thread-Ausführung und gewährleisten die Systemstabilität bei Hardwareausfällen.

\textbf{Adaptierung für die Webots-Simulation:} Die Portierung von Zanders Balance-Regelung in die Webots-Simulationsumgebung 
erforderte eine systematische Vereinfachung der Hardware-spezifischen Komplexitäten bei Beibehaltung der regelungstechnischen 
Kernprinzipien. Die wesentlichen Unterschiede zwischen Hardware- und Simulationsimplementierung zeigt Tabelle~\ref{tab:zander_comparison}.

\begin{table}[H]
\centering
\caption{Vergleich zwischen Zanders Hardware-Implementierung und Webots-Simulation}
\label{tab:zander_comparison}
\begin{tabular}{|l|l|l|}
\hline
\textbf{Aspekt} & \textbf{Zander (Hardware)} & \textbf{Webots-Simulation} \\
\hline
Sensorik & BNO055 IMU, Rauschen & Idealer Rollwinkelsensor \\
Threading & Multi-threaded (200/1000 Hz) & Synchrone Hauptschleife \\
Lenkpositionsregelung & Separater MD49-PID & Direkter Motoranschluss \\
PID-Parameter & $K_p=10, K_i=0, K_d=2.2$ & Identische Werte \\
Ausgangsbegrenzung & $\pm 150$ (Hardware-Einheiten) & $\pm 0.3$ rad \\
\hline
\end{tabular}
\end{table}

Die Webots-Implementierung vereinfacht die Architektur erheblich durch Elimination der sekundären Lenkpositionsregelung. 
Die zentrale Balance-Regelschleife lässt sich mathematisch als vierstufiger Prozess beschreiben:

\begin{align}
\text{Schritt 1: } \quad &\phi(t) = \text{get\_roll\_rad\_raw}() \label{eq:step1} \\
\text{Schritt 2: } \quad &u_{\text{raw}}(t) = \begin{cases}
-\text{PID}(\phi_{\text{soll}} - \phi(t), t) & \text{if control\_enabled} \\
0 & \text{sonst}
\end{cases} \label{eq:step2} \\
\text{Schritt 3: } \quad &u_{\text{sat}}(t) = \begin{cases}
\delta_{\max} & \text{if } u_{\text{raw}}(t) > \delta_{\max} \\
-\delta_{\max} & \text{if } u_{\text{raw}}(t) < -\delta_{\max} \\
u_{\text{raw}}(t) & \text{sonst}
\end{cases} \label{eq:step3} \\
\text{Schritt 4: } \quad &\text{wb\_motor\_set\_position}(\text{motor::handlebars}, u_{\text{sat}}(t)) \label{eq:step4}
\end{align}

wobei $\phi(t)$ der gemessene Roll-Winkel, $\phi_{\text{soll}} = 0$ der Sollwert, $u_{\text{raw}}(t)$ das ungefilterte PID-Signal, $u_{\text{sat}}(t)$ das begrenzte Stellsignal und $\delta_{\max}$ die maximale Lenkwinkel-Begrenzung darstellen. Schritt 4 zeigt die direkte Motoransteuerung ohne sekundäre Regelung durch \texttt{wb\_motor\_set\_position()}.

Der PID-Algorithmus selbst wurde von Zanders Implementierung adaptiert, jedoch mit verbesserter numerischer Stabilität. 
Die vereinfachte PID-Berechnung lässt sich mathematisch wie folgt beschreiben:

\begin{align}
\text{Zeitschritt: } \quad &\Delta t = \frac{t[n] - t[n-1]}{10^6} \text{ [s]} \label{eq:webots_dt} \\
\text{P-Term: } \quad &P(n) = K_p \cdot e(n) \label{eq:webots_p} \\
\text{I-Term: } \quad &I(n) = \begin{cases}
I(n-1) + K_i \cdot e(n) \cdot \Delta t & \text{falls } 0 < \Delta t < 1 \\
I(n-1) & \text{sonst}
\end{cases} \label{eq:webots_i} \\
\text{D-Term: } \quad &D(n) = K_d \cdot \frac{1}{L} \sum_{j=0}^{L-1} \frac{e(n-j) - e(n-j-1)}{\Delta t} \label{eq:webots_d} \\
\text{Ausgabe: } \quad &u(n) = P(n) + I(n) + D(n) \label{eq:webots_output}
\end{align}

wobei $e(n) = \phi_{\text{soll}} - \phi(n)$ der Roll-Winkelfehler, $t[n]$ der Zeitstempel in Mikrosekunden und $L$ die Pufferlänge (\texttt{HISTORY\_LEN}) darstellen. Die Besonderheiten der Implementierung sind:

\begin{itemize}
\item \textbf{Robuste Integration:} Der I-Term wird nur bei plausiblen Zeitschritten ($0 < \Delta t < 1$) aktualisiert
\item \textbf{Geglättete Ableitung:} Der D-Term verwendet einen gleitenden Durchschnitt über $L$ vergangene Ableitungswerte zur Rauschunterdrückung
\item \textbf{Zirkulärer Puffer:} \texttt{prev\_index = (history\_counter + L - 1) \% L} implementiert effiziente Speicherverwaltung
\end{itemize}

Die erfolgreiche Übertragung von Zanders Regelungskonzept in die Simulation bestätigt 
die Robustheit des gewählten PID-Ansatzes. Die Parameter (\(K_p = 10\), \(K_i = 0\), \(K_d = 2{,}2\)) erzielen 
in der idealisierten Webots-Umgebung stabile Balance-Regelung. 

Die Portierung demonstriert, wie Hardware-spezifische Komplexitäten (Multi-Threading, Sensor-Rauschen, Aktor-Totzeiten) in 
einer Simulationsumgebung abstrahiert werden können, ohne die regelungstechnischen Kernprinzipien zu beeinträchtigen. 
Diese Vereinfachung schafft die Grundlage für die Integration zusätzlicher Regelungsebenen, insbesondere der vision-basierten 
Pfadführung, die in den folgenden Abschnitten behandelt wird.


  
\subsection{Portierung des Fahrtrichtungs-MPC (Giray)} \label{chap:MPC_KAPITEL}
Yasin Giray entwickelte in seiner Masterarbeit einen vision-basierten 
Fahrtrichtungsregler auf Basis von Model Predictive Control (MPC), 
um das selbstbalancierende Fahrrad autonom der Fahrspur folgen zu lassen \citep{vision_mpc_readme}. Dieser Regler modelliert die Fahrzeugdynamik mittels Einspurmodell 
und nutzt zwei Steuergrößen, die aktuelle Geschwindigkeit \(v\) und Lenkwinkel \(\phi\) als Eingaben für die Prädiktion. Der MPC berechnet 
also auf Grundlage von \(v\) und Lenkeinschlag die Fahrzeugbewegung voraus. Für den prädiktiven Regler wird ein 
Zustandsvektor \(x=[x, y, v, \text{yaw}]\) (Position, Geschwindigkeit, Gierwinkel) und ein Eingangsvektor \(u=[\text{accel}, \text{steer}]\) 
definiert. Giray wählte einen Zeithorizont von etwa 2~s und formulierte ein Optimierungsproblem, das die Abweichung von einer Soll-Trajektorie 
minimiert sowie Steuerungsaufwand und -änderungen übernimmt. Die Kostenfunktion des MPC addiert quadrierte Abweichungen der Zustände und Eingriffe über den Horizont sowie eine 
Endzustands-Strafe, z.\,B. \(J=\sum (x^T Q x + u^T R u + \Delta u^T R_d \Delta u) + x_{\text{final}}^T Q_f x_{\text{final}}\) \citep{vision_mpc_readme}. 
Neben den Kosten werden physikalische Nebenbedingungen wie maximaler Lenkwinkel, Lenkwinkelrate und Geschwindigkeit im Optimierungsproblem 
berücksichtigt, um realistische Lösungen zu gewährleisten.

Wesentliche Voraussetzung für den Fahrtrichtungs-MPC ist eine geeignete Referenztrajektorie der Straße. Giray implementierte hierfür eine 
Computer-Vision-Pipeline auf Basis von YOLOv8-Segmentierung, welche im Kamerabild die befahrbare Straße und deren Verlauf erkennt. Aus den 
segmentierten Bilddaten wird eine Reihe von Stützpunkten der Fahrbahn extrahiert. Anschließend 
wird mittels Spline-Interpolation eine glatte Kurve durch diese Punkte gelegt, die als Referenzkurve für den MPC dient. Er simuliert auf Basis des Einspurmodells die Fahrzeugbewegung und berechnet die optimalen 
Steuerbefehle, damit das Fahrrad der Kurve bestmöglich folgt. In Girays Umsetzung wird der Regler zyklisch mit ca. 10 Hz ausgeführt (Tickrate 100 ms). 
Pro Zyklus initialisiert der Controller den aktuellen Fahrzeugzustand (Position, Geschwindigkeit, Orientierung) und berechnet aus der ersten Sollkurve 
einen Vorhersagepfad. Die Optimierung liefert dann eine Stellgrößen-Sequenz für Beschleunigung und Lenkwinkel. 
Tatsächlich wird nur der erste Lenkwinkel-Impuls umgesetzt (Receding Horizon Control), welcher als neuer Lenkbefehl an das Fahrzeug geht. 
Im nächsten Zyklus wird der MPC erneut mit aktualisiertem Zustand und ggf. angepasster Referenz gestartet. Giray konnte in Simulationen zeigen, 
dass der MPC-Regler das Fahrrad effektiv der vorgegebenen Spur folgen lässt. Die geplante Spline-Trajektorie und die vom MPC gefahrene 
Ist-Trajektorie in seiner Arbeit, stimmen dabei eng überein, was die Güte der prädiktiven Regelung belegt.


\textbf{Adaptierung für Webots-Simulation:} Die Portierung von Girays MPC-Konzept in die Webots-Umgebung erforderte eine systematische 
Anpassung an die spezifischen Anforderungen der Simulation und Integration mit dem vorhandenen Balance-Controller. Tabelle~\ref{tab:giray_comparison} 
zeigt die wesentlichen Unterschiede zwischen Girays theoretischem Ansatz und der Webots-Implementierung.

\begin{table}[H]
\centering
\caption{Vergleich zwischen Girays MPC-Konzept und Webots-Implementierung}
\label{tab:giray_comparison}
\begin{tabular}{|l|l|l|}
\hline
\textbf{Aspekt} & \textbf{Giray (Theorie)} & \textbf{Webots-Implementierung} \\
\hline
Prädiktionshorizont & N=20 (2\,s - 10\,Hz) & T=3 (0.3\,s - 20\,Hz) \\
Referenzgenerierung & Spline-Interpolation & Vereinfachte Korrekturtrajectorie \\
Zustandsvektor & $[x, y, v, \psi]^T$ & $[x, y, v, yaw]^T$ (identisch) \\
Eingangsvektor & $[a, \delta]^T$ & $[accel, steer]^T$ (identisch) \\
Regelfrequenz & 10\,Hz & 20\,Hz \\
Optimierer & CVXPY (theoretisch) & CVXPY (praktisch) \\
Integration & Standalone & Gekoppelt mit Balance-PID \\
\hline
\end{tabular}
\end{table}

Die Webots-Implementierung vereinfacht Girays Ansatz erheblich durch Reduzierung des Prädiktionshorizonts auf 3 Zeitschritte 
(\(\approx 0{,}3\,\mathrm{s}\)). Diese Entscheidung wurde aus Echtzeitgründen getroffen, da die Integration mit dem Balance-Controller 
eine höhere Regelfrequenz (20\,Hz) erfordert. Bei niedrigeren Regelfrequenzen würde die Regelung instabiler werden, da das System 
weniger Zeit für die Bildverarbeitung und MPC-Berechnung hätte, was zu verzögerten Reaktionen auf Pfadabweichungen führen würde. Listing~\ref{lst:webots_mpc_init} zeigt die Initialisierung des MPC-Controllers 
mit den angepassten Parametern:

\begin{lstlisting}[style=python,caption={MPC-Controller Initialisierung in der Webots-Implementierung},label={lst:webots_mpc_init}]
def __init__(self):
    # MPC-Parameter (reduziert gegenueber Giray)
    self.NX = 4  # Zustandsvektor: [x, y, v, yaw]
    self.NU = 2  # Eingaenge: [accel, steer]
    self.T = 3   # Praediktionshorizont (reduziert fuer Echtzeitfaehigkeit)
    
    # Kostenmatrizen (aehnlich Girays Formulierung)
    self.R = np.diag([0.01, 0.01])      # Eingangskostenmatrix
    self.Rd = np.diag([0.01, 1.0])      # Eingangsdifferenzkostenmatrix
    self.Q = np.diag([1.0, 1.0, 0.5, 0.5])  # Zustandskostenmatrix
    self.Qf = self.Q                    # Terminale Kostenmatrix
    
    # Fahrzeugparameter (Bicycle-Modell)
    self.WB = 1.2  # Radstand [m] (Fahrrad)
    self.DT = 0.1  # Zeitschritt [s]
    
    # Physikalische Begrenzungen
    self.MAX_STEER = math.radians(30.0)  # Maximaler Lenkwinkel
    self.MAX_DSTEER = math.radians(20.0) # Maximale Lenkwinkelrate
    self.MAX_SPEED = 8.0                 # Maximale Geschwindigkeit
    self.MIN_SPEED = 0.5                 # Minimale Geschwindigkeit
\end{lstlisting}

Anstelle von Girays komplexer Spline-Interpolation aus Bildpunkten verwendet die Webots-Implementierung eine vereinfachte 
Referenzgenerierung basierend auf dem aktuellen Vision-Fehler. Die Referenztrajektorie wird aus dem lateralen Spurabweichungsfehler 
abgeleitet, indem ein Ziel-Gierwinkel berechnet und über den kurzen Horizont interpoliert wird.

\textbf{MPC-Optimierungsalgorithmus:} Die Webots-Implementierung folgt Girays theoretischer Formulierung bei der Lösung des 
Optimierungsproblems. Abbildung~\ref{fig:mpc_blockdiagram} zeigt die strukturierte Darstellung des MPC-Algorithmus:

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=1.2cm and 1.5cm,
  block/.style={rectangle, draw, fill=blue!10, text width=2.8cm, text centered, minimum height=1cm},
  decision/.style={diamond, draw, fill=yellow!20, text width=2cm, text centered, minimum height=1cm},
  input/.style={rectangle, draw, fill=green!10, text width=2.5cm, text centered, minimum height=0.8cm},
  output/.style={rectangle, draw, fill=red!10, text width=2.5cm, text centered, minimum height=0.8cm},
  constraint/.style={rectangle, draw, fill=orange!10, text width=2.8cm, text centered, minimum height=0.7cm, font=\footnotesize},
  constraint_box/.style={rectangle, draw=orange!60, fill=orange!5, thick, dashed, minimum height=2cm, minimum width=16cm},
  arrow/.style={->, >=stealth, thick}
]

% Eingaben, Variablen und Kostenfunktion nebeneinander
\node[input] (inputs) at (-3.5,2.2) {Eingaben\\$x_{\text{ref}}, x_0, T$};

% Variablendefinition
\node[block] (variables) at (0,2.2) {Optimierungsvar.\\$x \in \mathbb{R}^{4 \times (T+1)}$\\$u \in \mathbb{R}^{2 \times T}$};

% Kostenfunktion
\node[block] (cost) at (3.5,2.2) {Kostenfunktion\\$J = J_{\text{track}} + J_{\text{control}} + J_{\text{rate}} + J_{\text{terminal}}$};

% Nebenbedingungen-Container
\node[constraint_box, below=1.2cm of cost] (constraint_container) {};
\node[above=0.05cm of constraint_container.north, font=\small\bfseries, color=orange!80] {Nebenbedingungen (Constraints)};

% Nebenbedingungen - Kompaktere Abstände
\node[constraint] (dynamics) at ([xshift=-6.2cm]constraint_container.center) {\textbf{Systemdynamik}\\$x[t+1] = A_t x[t] + B_t u[t] + C_t$\\$\forall t = 0, \ldots, T-1$};
\node[constraint] (bounds) at ([xshift=-3.1cm]constraint_container.center) {\textbf{Stellbegrenzung}\\$|u_a[t]| \leq 1.0$ m/s$^2$\\$|\delta[t]| \leq 0.3$ rad\\$\forall t = 0, \ldots, T-1$};
\node[constraint] (rate) at (constraint_container.center) {\textbf{Lenkbegrenzung}\\$|\delta[t+1] - \delta[t]| \leq 0.3 \Delta t$ rad/s\\$\forall t = 0, \ldots, T-2$};
\node[constraint] (speed) at ([xshift=3.1cm]constraint_container.center) {\textbf{Speedlimits}\\$0.5 \leq v[t] \leq 3.0$ m/s\\$\forall t = 0, \ldots, T$};
\node[constraint] (initial) at ([xshift=6.2cm]constraint_container.center) {\textbf{Init}\\$x[0] = x_0$};

% Solver    
\node[block, below=1.2cm of constraint_container] (solver) {cvxpy.solve()\\Konvexe Optimierung};

% Entscheidung
\node[decision, below=0.8cm of solver] (decision) {Optimal?};

% Ausgaben
\node[output, below left=0.8cm and 1.2cm of decision] (output_opt) {Optimale Lösung\\$(u_a^*[0], \delta^*[0])$};
\node[output, below right=0.8cm and 1.2cm of decision] (output_fallback) {Fallback-Regelung\\$u_{\text{simple}}$};

% Hauptfluss-Pfeile
\draw[arrow] (inputs) -- (variables);
\draw[arrow] (variables) -- (cost);
\draw[arrow] (cost) -- (constraint_container.north);
\draw[arrow] (constraint_container.south) -- (solver);
\draw[arrow] (solver) -- (decision);

% Entscheidungsausgänge
\draw[arrow] (decision) -- node[left] {Ja} (output_opt);
\draw[arrow] (decision) -- node[right] {Nein} (output_fallback);

\end{tikzpicture}
\caption{MPC-Optimierungsalgorithmus mit gebündelten Nebenbedingungen}
\label{fig:mpc_blockdiagram}
\end{figure}

Die vollständige Implementierung des MPC-Optimierungsalgorithmus ist in Listing~\ref{lst:webots_mpc_solver_appendix} in den Anlagen dargestellt.

Die Kostenfunktion $J$ setzt sich aus folgenden Komponenten zusammen:

\begin{itemize}
    \item \textbf{Tracking-Term:} 
    \begin{equation}
        J_{\text{track}} = \sum_{t=1}^{T} \|x_{\text{ref}}[t] - x[t]\|_Q^2
    \end{equation}
    Bestraft Abweichungen von der Referenztrajektorie.
    \item \textbf{Stellaufwand:} 
    \begin{equation}
        J_{\text{control}} = \sum_{t=0}^{T-1} \|u[t]\|_R^2
    \end{equation}
    Begrenzt die Stellgrößen und verhindert übermäßige Stellaufwände.
    
    \item \textbf{Stellraten-Gewichtung:} 
    \begin{equation}
        J_{\text{rate}} = \sum_{t=0}^{T-2} \|u[t+1] - u[t]\|_{R_d}^2
    \end{equation}
    Sorgt für glatte Übergänge und verhindert abrupte Änderungen der Stellgrößen.
    
    \item \textbf{Terminal-Term:} 
    \begin{equation}
        J_{\text{terminal}} = \|x_{\text{ref}}[T] - x[T]\|_{Q_f}^2
    \end{equation}
    Gewichtet den Endzustand stärker und stellt sicher, dass das System am Ende des Prädiktionshorizonts nahe der Referenz ist.
\end{itemize}

Die Implementierung folgt dem Standard-MPC-Schema mit quadratischen Kosten und linearen Constraints. Im Gegensatz zu Girays 
längerfristigem Horizont ermöglicht die Reduzierung auf 3 Zeitschritte eine Echtzeitausführung mit 20\,Hz.

\textbf{Integration mit Balance-Controller:} Im Gegensatz zu Girays Standalone-Ansatz realisiert die Webots-Implementierung 
eine zweistufige Kaskadenregelung (Abbildung \ref{fig:cascade_controller}). Der Vision Controller (MPC, 20\,Hz) übernimmt die 
Spurführung basierend auf dem Kamerabild, während der Balance Controller (PID, 500\,Hz) die Stabilisierung anhand des Rollwinkels 
gewährleistet. Die MPC-Ausgaben werden über IPC an den C-basierten Balance-Controller übertragen, der zusätzlich einen separaten 
Soll-Rollwinkel-Eingang erhält (siehe Abbildung \ref{fig:cascade_controller}).


\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=2cm and 3.5cm,
    block/.style={rectangle, draw, fill=blue!10, text width=2.5cm, text centered, minimum height=1.2cm, font=\footnotesize},
    vision_block/.style={rectangle, draw, fill=green!15, text width=2.5cm, text centered, minimum height=1.2cm, font=\footnotesize},
    balance_block/.style={rectangle, draw, fill=orange!15, text width=2.5cm, text centered, minimum height=1.2cm, font=\footnotesize},
    sensor_block/.style={rectangle, draw, fill=purple!10, text width=2.2cm, text centered, minimum height=1cm, font=\footnotesize},
    summing/.style={circle, draw, minimum size=0.8cm, font=\footnotesize},
    arrow/.style={->, >=stealth, thick},
    stage_label/.style={font=\footnotesize\bfseries, text=red!70!black}
]

% Stage Labels - positioned further left
\node[stage_label] at (-3.5, 0.25) {Stufe 1: 20Hz};
\node[stage_label] at (-3.4, -0.25) {Stufe 2: 500Hz};

% Reference input - moved further left
\node[coordinate] (ref_input) at (-3, 1.5) {};
\node[left] at (ref_input) {\footnotesize Sollpfad $r_{path}(t)$};

% Soll-Rollwinkel input for Stage 2
\node[coordinate] (roll_ref_input) at (-3, -2.5) {};
\node[left] at (roll_ref_input) {\footnotesize Soll-Rollwinkel $r_{roll}(t)$};

% Stage 1: Vision Controller (MPC) - repositioned
\node[vision_block] (vision_controller) at (1, 1.5) {Vision Controller\\(MPC)\\Python};
\node[below=0.1cm of vision_controller, font=\tiny] {vision\_control\_py};

% Summing junction for Stage 1 - moved left
\node[summing] (sum1) at (-2, 1.5) {$+$};
\node[above left=0.15cm of sum1] {\tiny $-$};

% IPC Communication - moved right
\node[block, fill=gray!10] (ipc) at (5, 1.5) {IPC\\Kommunikation};

% Stage 2: Balance Controller (PID) - repositioned
\node[balance_block] (balance_controller) at (1, -2.5) {Balance Controller\\(PID)\\C};
\node[below=0.1cm of balance_controller, font=\tiny] {balance\_control\_c};

% Summing junction for Stage 2 - moved left
\node[summing] (sum2) at (-2, -2.5) {$+$};
\node[above left=0.15cm of sum2] {\tiny $-$};

% Plant (Bicycle) - moved right
\node[block, fill=yellow!15] (plant) at (8, -2.5) {Fahrrad\\(Webots)};

% Feedback sensors - better positioned
\node[sensor_block] (camera) at (1, 4) {Kamera\\(Pfaderkennung)};
\node[sensor_block] (imu) at (8, -5) {IMU\\(Balance)};

% Arrows - Stage 1 (dünnere Pfeile für Sollwerte)
\draw[->, >=stealth, thin] (ref_input) -- (sum1);
\draw[arrow] (sum1) -- (vision_controller);
\draw[arrow] (vision_controller) -- node[above, font=\scriptsize] {$u_{path}$} (ipc);

% Soll-Rollwinkel to Stage 2 (dünnere Pfeile für Sollwerte)
\draw[->, >=stealth, thin] (roll_ref_input) -- (sum2);

% IPC to Stage 2 - cleaner routing
\draw[arrow] (ipc) -- ++(0,-1.2) node[right, font=\scriptsize] {$r_{balance}$} -- ++(0,-1.3) -| (sum2);

% Arrows - Stage 2
\draw[arrow] (sum2) -- (balance_controller);
\draw[arrow] (balance_controller) -- node[above, font=\scriptsize] {$u_{motor}$} (plant);

% Feedback paths - cleaner routing
\draw[arrow] (plant) |- node[near start, right, font=\scriptsize] {Kamerabild} (camera);
\draw[arrow] (camera) -| node[near start, above, font=\scriptsize] {$y_{path}$} (sum1);

\draw[arrow] (plant) -- node[right, font=\scriptsize] {Rollwinkel} (imu);
\draw[arrow] (imu) -| node[near start, below, font=\scriptsize] {$y_{balance}$} (sum2);

% Stage separation line - extended
\draw[dashed, gray, thick] (-6, 0) -- (9.5, 0);



\end{tikzpicture}
\caption{Zweistufige Kaskadenregelung: Vision-MPC (Stufe 1) für Spurführung und Balance-PID (Stufe 2) für Stabilisierung}
\label{fig:cascade_controller}
\end{figure}




\subsection{Strategien der Spurführung}

Die Vision-basierte Spurführung des selbstbalancierenden Fahrrads nutzt zwei unterschiedliche Erkennungsstrategien: 
eine YOLO-basierte Segmentierung als Hauptverfahren und eine robuste Fallback-Strategie für Ausfallsituationen. 
Beide Verfahren extrahieren aus dem Kamerabild einen lateralen Spurabweichungsfehler, der vom zuvor beschriebenen MPC 
in Lenkbefehle umgesetzt wird.

\subsubsection{YOLO-Segmentierung (YOLOv8)}

Die YOLO-Strategie nutzt ein vortrainiertes YOLOv8-Segmentierungsmodell zur robusten Erkennung der Fahrbahn. 
Das neuronale Netz identifiziert Straßenbereiche als Klasse \enquote{street\_main} und liefert sowohl Bounding Boxes 
als auch pixelgenaue Segmentierungsmasken, wie in Kapitel \ref{chap:yolo} beschrieben ist.
Das System sucht beim Start nach einer trainierten Gewichtsdatei (\texttt{best.pt}) 
und lädt das YOLOv8-Modell auf das verfügbare Rechengerät.



Im Echtzeitbetrieb (20\,Hz) analysiert YOLO jedes Kamerabild und filtert Straßenobjekte 
(Klasse \enquote{street\_main}). Aus den Bounding Boxes aller erkannten Straßenbereiche wird der gemittelte Mittelpunkt berechnet. Die laterale 
Abweichung ergibt sich als normierte Differenz zwischen Straßenmittelpunkt und Bildmitte (\texttt{error} $\in [-0.5, +0.5]$):

\begin{figure}[H]
  \centering
  \includegraphics[width=0.8\textwidth]{figures/Yolo_Snapshot.png}
  \caption{YOLOv8-Segmentierung.}
  \label{fig:yolov8}
\end{figure}


Die YOLO-Fehlerberechnung erfolgt durch folgende mathematische Schritte:

\textbf{1. Zentrumsberechnung der Straßen-Bounding-Boxes:}
\begin{equation}
    x_{center,i} = \frac{x_{min,i} + x_{max,i}}{2} \quad \text{für alle Straßen-Detektionen } i
\end{equation}
Berechnet das horizontale Zentrum jeder von YOLO erkannten Straßen-Bounding-Box.

\textbf{2. Durchschnittliches Straßenzentrum:}
\begin{equation}
    \bar{x}_{street} = \frac{1}{N} \sum_{i=1}^{N} x_{center,i}
\end{equation}
Mittelt alle Straßenzentren zu einem einzigen repräsentativen Straßenzentrum.

\textbf{3. Normierter Lateral-Fehler:}
\begin{equation}
    e_{lateral} = \frac{x_{frame\_center} - \bar{x}_{street}}{W_{frame}}
\end{equation}
Berechnet den normierten Abstand zwischen Bildmitte und durchschnittlichem Straßenzentrum.

Hierbei ist:
\begin{itemize}
    \item $x_{frame\_center} = \frac{W_{frame}}{2}$ das Bildzentrum
    \item $W_{frame}$ die Bildbreite in Pixeln
    \item $e_{lateral} \in [-0.5, 0.5]$ der normierte Lateral-Fehler
\end{itemize}

\textbf{Interpretation:}
\begin{itemize}
    \item $e_{lateral} = 0$: Straße ist zentriert im Bild
    \item $e_{lateral} > 0$: Straße ist links vom Bildzentrum → Lenkung nach rechts erforderlich
    \item $e_{lateral} < 0$: Straße ist rechts vom Bildzentrum → Lenkung nach links erforderlich
\end{itemize}

Die detaillierte Implementierung der YOLO-Fehlerberechnung ist in Anlage~\ref{lst:yolo_error_calculation_appendix} dargestellt.



Das System implementiert eine Ausfallüberbrückung durch Zwischenspeicherung 
der letzten gültigen Fehlerwerte. Bei Detektionsaussetzern wird der gespeicherte Fehler mit Decay-Faktor\footnote{Der Decay-Faktor reduziert den gespeicherten Fehlerwert in jedem Zyklus um einen festen Prozentsatz, um ein allmähliches Abklingen zu gewährleisten.} schrittweise 
abgeschwächt, bis er nach definierten Ausbleib-Zyklen auf Null fällt. Dies verhindert abrupte Lenkbewegungen bei 
temporären Erkennungsverlusten.




\textbf{Limitierungen:} Die YOLO-Strategie erfordert ein geeignetes vortrainiertes Modell und ausreichende 
Rechenleistung. Bei Abweichungen von den Trainingsdaten können Fehlerkennungen auftreten. Diese Fehlererkennungen 
werden in Kapitel \ref{chap:teststrecken} beschrieben. 



\subsubsection{Fallback-Strategie (ROI-basiert)}

Bei Nichtverfügbarkeit des YOLO-Modells aktiviert sich automatisch eine Fallback-Strategie, die auf klassischer 
Bildverarbeitung basiert. Diese nutzt Farb- und Konturfilter innerhalb einer definierten Region of Interest (ROI) 
zur Straßenerkennung, dabei mit geringerer Präzision, aber ohne Abhängigkeit von trainierten Modellen, bei 
denen zu fehlern bei der Straßenerkennung kommen kann.

\begin{figure}[H]
  \centering
  \includegraphics[width=1.0\textwidth]{figures/Fallback_snapshot.png}
  \caption{Fallback-Strategie.}
  \label{fig:fallback}
\end{figure}

Das System definiert eine trapezförmige ROI (ca.\ 40\% Bildhöhe, 2\% Breite) zur 
Fokussierung auf den erwarteten Fahrbahnbereich. Innerhalb dieser ROI erfolgt HSV-basierte Farbsegmentierung für 
dunkelgraue Straßenbereiche und weiße Markierungen, gefolgt von morphologischen Operationen zur Rauschreduktion. 
Die größte erkannte Kontur wird als Straßenbereich interpretiert:




Die Fallback-Fehlerberechnung basiert auf HSV-Farbsegmentierung und erfolgt in folgenden mathematischen Schritten:

\textbf{1. HSV-Farbbereich-Filterung:}
\begin{align}
    M_{road}(x,y) &= \begin{cases} 
        255 & \text{falls } HSV_{lower,road} \leq HSV(x,y) \leq HSV_{upper,road} \\
        0 & \text{sonst}
    \end{cases} \\
    M_{white}(x,y) &= \begin{cases} 
        255 & \text{falls } HSV_{lower,white} \leq HSV(x,y) \leq HSV_{upper,white} \\
        0 & \text{sonst}
    \end{cases}
\end{align}
Das Eingangsbild wird vom RGB- in den HSV-Farbraum konvertiert, da dieser robustere Farbsegmentierung bei 
wechselnden Lichtverhältnissen ermöglicht. Für jeden Pixel wird geprüft, ob seine HSV-Werte innerhalb der 
definierten Bereiche für Straßenoberfläche (grau) oder Fahrbahnmarkierungen (weiß) liegen. Entsprechende 
Pixel erhalten in der jeweiligen Binärmaske den Wert 255 (weiß), alle anderen 0 (schwarz).

\textbf{2. Maskenkombination und morphologische Operationen:}
\begin{align}
    M_{combined} &= M_{road} \vee M_{white} \\
    M_{closed} &= \text{Close}(M_{combined}, K) \\
    M_{final} &= \text{Open}(M_{closed}, K)
\end{align}
Die beiden Binärmasken werden durch logisches ODER verknüpft, um eine vereinte Straßenmaske zu erzeugen. 
Anschließend werden morphologische Operationen mit einem strukturierenden Element K (typisch 5×5 Kernel) 
angewendet: Die Schließung\footnote{Close (Schließung): Morphologische Operation, die kleine Löcher in Objekten füllt und Konturen glättet. Entspricht einer Dilatation gefolgt von einer Erosion.} füllt kleine Lücken in der Straßenkontur und verbindet getrennte Bereiche, 
während die Öffnung\footnote{Open (Öffnung): Morphologische Operation, die kleine Objekte und Rauschen entfernt. Entspricht einer Erosion gefolgt von einer Dilatation.} störende kleine Objekte und Bildrauschen eliminiert, ohne die 
Hauptkonturen zu beeinträchtigen.\newline
\textbf{3. Konturerkennung und Zentrumsberechnung:}
\begin{align}
    C_{max} &= \arg\max_{C \in \mathcal{C}} \text{Area}(C) \\
    (x, y, w, h) &= \text{BoundingRect}(C_{max}) \\
    x_{center} &= x + \frac{w}{2}
\end{align}
Aus der gefilterten Maske werden alle geschlossenen Konturen extrahiert und nach ihrer Fläche sortiert. 
Die größte Kontur wird als Hauptstraßenbereich interpretiert, da sie typischerweise den dominanten 
fahrbaren Bereich repräsentiert. Um deren horizontales Zentrum zu bestimmen, wird eine achsenparallele 
Bounding Box berechnet und deren Mittelpunkt in x-Richtung als Straßenzentrum verwendet.

\textbf{4. Normierter Lateral-Fehler:}
\begin{equation}
    e_{fallback} = \frac{x_{target} - x_{center}}{W_{frame}}
\end{equation}
Der finale Regelfehler wird als normierter Abstand zwischen der gewünschten Zielposition (meist Bildmitte) 
und dem detektierten Straßenzentrum berechnet. Die Normierung durch die Bildbreite $W_{frame}$ stellt 
sicher, dass der Fehler unabhängig von der Auflösung im Bereich [-1, 1] liegt, wobei negative Werte 
eine Abweichung nach links und positive Werte eine Abweichung nach rechts signalisieren.

Hierbei ist:
\begin{itemize}
    \item $x_{target}$ die gewünschte Zielposition (meist Bildmitte)
    \item $x_{center}$ das berechnete Zentrum der größten Straßenkontur
    \item $W_{frame}$ die Bildbreite zur Normierung
    \item $e_{fallback} \in [-1, 1]$ der normierte Lateral-Fehler
\end{itemize}


\textbf{Vorteile der Fallback-Methode:}
\begin{enumerate}
    \item Robuste Farbsegmentierung bei konstanten Lichtverhältnissen
    \item Geringe Rechenleistung erforderlich
    \item Deterministisches Verhalten ohne ML-Unsicherheiten
    \item Morphologische Filterung reduziert Rauschen
\end{enumerate}

Die detaillierte Implementierung der Fallback-Fehlerberechnung ist in Anlage~\ref{lst:fallback_error_calculation_appendix} dargestellt.



Der Algorithmus ermittelt die größte Kontur als Straßenbereich und berechnet deren Mittelpunkt. Die normierte 
Differenz zur Bildmitte ergibt den lateralen Fehler, analog zur YOLO-Methode. Bei Erkennungsverlusten greift 
die gleiche Decay-Logik wie in der YOLO-Strategie.

\textbf{Limitierungen:} Die Fallback-Strategie ist anfälliger für variierende Lichtverhältnisse und 
unkonventionelle Straßenbeläge, da sie auf feste Farbschwellenwerte angewiesen ist.
Trotz geringerer Präzision gewährleistet sie grundlegende Spurhaltung 
als Notfallmodus bei YOLO-Ausfall und ist besonders gut für vordefinierte Simulationswege geeignet.



